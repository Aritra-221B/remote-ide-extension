---
description: Run the application tests
---
1. Run `npm install` to ensure all dependencies are resolved.
// turbo
2. Run `npm run build` to compile the TypeScript extensions.
// turbo
3. Run `node test/smoke.js` to run the smoke test standalone server and verify correct route responses.
